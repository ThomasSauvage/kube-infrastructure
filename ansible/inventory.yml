servers:
  children:
    proxmox_node: {}
    proxmox_containers: {}

proxmox_node:
  hosts:
    brot-1:
      ansible_ssh_host: brot-1
    brot-2:
      ansible_ssh_host: brot-2

# For non-Talos proxmox containers
proxmox_containers:
  hosts:
