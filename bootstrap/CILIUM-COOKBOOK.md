# Cilium bootstrap

Those kubernetes manifests allow to bootstrap the creation of the k8s cluster.

## Bootstraping Cilium

- Install cilium cli on your computer

```sh
brew install cilium-cli
```

- Install Cilium on the cluster

```sh
./cilium.helm.sh
```
